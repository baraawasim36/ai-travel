# Travel Explorer AI — Agent Execution Pack v2

This pack is aligned to the repository audit report supplied from the coding agent. It deliberately puts P0 build/security/backend integrity ahead of product feature work.

Start with `AGENTS.md`, then `docs/00-execution-source-of-truth.md`, then run the Phase 0 prompt in `docs/09-agent-first-prompt.md`.

The current inherited backend baseline is Supabase. Do not introduce a Firebase migration unless a separate architecture decision explicitly approves it.
